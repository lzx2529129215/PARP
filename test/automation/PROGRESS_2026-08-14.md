# Automation 扩展进度（2026-08-14）

> 状态：目录结构调整已完成，自动化、独立 test 和常驻 service 已完成一轮联调。`lzx-note`

## 当前范围

当前自动化根目录为 `/home/lzx/Desktop/PARP/test/automation`，场景配置为 `/home/lzx/Desktop/PARP/test/configs/automation`；常驻采集位于 `/home/lzx/Desktop/PARP/lzx/service/runtime_monitor`。

## 已完成

1. `app_automation.py` 已增加下列窗口识别与应用映射：
   - Calendar：`CALENDAR`
   - Rhythmbox：`RHYTHMBOX`
   - Image Viewer / EOG：`IMAGE_VIEWER`
   - Shotwell：`SHOTWELL`
   - GNOME System Monitor：`SYSTEM_MONITOR`
   - AisleRiot Solitaire：`SOLITAIRE`
   - Epiphany 可作为免登录浏览器窗口映射到 `FIREFOX`
2. 新增免登录六应用场景：
   - `configs/automation/scenario_login_free_six_apps.json`
   - 入口：`automation/run_rich_scenario.sh login-free-six`
   - 每个应用覆盖启动、内容窗口等待、两轮切换、前台验证、应用内按键和 cgroup 安全关闭。
3. 新增确定性本地素材生成器：
   - `automation/scripts/create_login_free_assets.py`
   - 生成 PNG、WAV、含本地曲目的隔离 `rhythmdb.xml` 和 Shotwell 数据目录。
4. 新增 trace 验证器：
   - `automation/scripts/verify_login_free_apps.py`
   - 严格检查 6 个应用的成功启动、切换/聚焦、前台验证、场景完成标志和失败动作数。
5. `run_rich_scenario.sh`、`README.md` 和自动化单测已经同步更新。

## 已完成验证

- Python/Bash/JSON 静态检查通过。
- `bash test/automation/run-tests-lzx.sh`：18 项全部通过。
- dry-run：71 个展开动作全部可执行。
- 第一轮完整 Xvfb 实跑及验证器结果：`PASS`，6/6 应用通过，0 个失败动作；结果：
  - `outputs/automation_login_free/verification_20260814/automation_trace.csv`
  - `outputs/automation_login_free/verification_20260814/verification.json`
- 最新完整 Xvfb 实跑已正常退出（Rhythmbox 使用含本地 WAV 的隔离音乐库）：
  - `outputs/automation_login_free/verification_final2_20260814/automation_trace.csv`
  - trace 中 `SCENARIO_DONE=1`、失败动作数 `0`
  - 尚未为这一轮生成单独的 `verification.json`，因为用户要求立即暂停。

## 实跑中已修复的问题

- 素材准备最初被当作异步 cgroup shell；已改为同步执行，确保从空输出目录启动也不会与 `assert_file` 竞争。
- Shotwell 不支持原 PPM 素材；当前生成标准 PNG，实际内容窗口为 1024×768，已成功操作。
- Rhythmbox 的 `--no-registration` 不接受命令行媒体文件；当前通过隔离 `rhythmdb.xml` 注入本地 WAV。
- Rhythmbox 的 `Down + Return` 会把焦点导向侧栏并隐藏主窗口；已移除，保留两轮稳定的播放/暂停按键。

## 暂停点与恢复事项

1. 等目录结构调整完成后，更新场景、README 和输出记录中的路径。
2. 对 `verification_final2_20260814/automation_trace.csv` 运行验证器并保存最终 JSON。
3. 重新跑一次静态检查和 6 应用 Xvfb 场景，确认目录迁移没有破坏相对路径。

## 清理状态

暂停时已停止测试用 Xvfb/Openbox；没有正在运行的 `automation-*.scope` 应用进程。
