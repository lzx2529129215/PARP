From 718cb52b6fd308dd090c7c1ebdd42f9b01de1e1c Mon Sep 17 00:00:00 2001
From: Codex PARP Reproduction <codex@localhost>
Date: Wed, 5 Aug 2026 14:38:21 +0800
Subject: [PATCH 117/123] parp: bound future access tracing to candidates

---
 ...rp_effective_tier_live_shadow_audit-lzx.md | 10 ++++++++++
 include/linux/parp.h                          |  8 ++++++++
 include/trace/events/parp.h                   | 17 ++++++++++++++--
 mm/parp/core/effective_tier.c                 | 20 +++++++++++++++----
 mm/parp/tests/parp_test.c                     | 13 ++++++++++++
 tools/parp/effective_tier/collector.py        |  7 +++++++
 tools/parp/effective_tier/contracts.py        |  4 +++-
 .../parp/effective_tier/raw_event.schema.json |  6 ++++--
 .../parp/effective_tier/tests/test_phase_e.py |  9 ++++++++-
 9 files changed, 84 insertions(+), 10 deletions(-)

diff --git a/docs/parp_effective_tier_live_shadow_audit-lzx.md b/docs/parp_effective_tier_live_shadow_audit-lzx.md
index e3217f448..662db52af 100644
--- a/docs/parp_effective_tier_live_shadow_audit-lzx.md
+++ b/docs/parp_effective_tier_live_shadow_audit-lzx.md
@@ -62,6 +62,16 @@ moves as future-access labels.  Live use remains gated on a post-boot smoke
 trace containing file, anon, candidate, trusted-access, outcome, batch, and
 lock events with cookie/lifetime/session linkage.
 
+### Candidate-correlated future-access trace <!-- #lzx -->
+
+The access trace is now deliberately bounded: it is emitted only for a
+trusted real-access event that observes a nonzero page-local reclaim-candidate
+count before that access clears the count.  The exported
+`candidate_count` must be positive; the offline collector fails closed on a
+missing or zero value.  Policy moves never produce this event.  This preserves
+the candidate-time to next-real-access join required by the ranking target and
+prevents global page-access trace flooding.
+
 ## Live boundary
 
 `tools/parp/effective_tier/live_shadow_preflight-lzx.py` is read-only. It records
diff --git a/include/linux/parp.h b/include/linux/parp.h
index 8654ae11c..0d176b6e5 100644
--- a/include/linux/parp.h
+++ b/include/linux/parp.h
@@ -382,6 +382,8 @@ unsigned long parp_effective_tier_policy_flags(unsigned long old_flags,
 		int new_generation);
 bool parp_effective_tier_claim_epoch(struct folio *folio, bool upgrade,
 		u16 epoch_tag, enum parp_tier_bypass_reason *bypass);
+bool parp_effective_tier_candidate_access_trace_eligible(bool real_access,
+		u8 candidate_count); /* #lzx */
 u64 parp_effective_tier_cookie(struct folio *folio);
 u32 parp_effective_tier_elapsed_ms(u32 now, u32 then, bool *valid);
 size_t parp_effective_tier_metadata_size(void);
@@ -524,6 +526,12 @@ static inline bool parp_effective_tier_tracking_active(void)
 	return false;
 }
 
+static inline bool parp_effective_tier_candidate_access_trace_eligible(
+		bool real_access, u8 candidate_count) /* #lzx */
+{
+	return false;
+}
+
 static inline bool parp_effective_tier_commit_revalidate(
 		const struct parp_tier_scan_ctx *ctx, struct folio *folio,
 		u16 state_sequence)
diff --git a/include/trace/events/parp.h b/include/trace/events/parp.h
index fb79924e0..67661edce 100644
--- a/include/trace/events/parp.h
+++ b/include/trace/events/parp.h
@@ -171,9 +171,17 @@ struct parp_effective_tier_access_trace {
 	s32 generation;
 	u8 page_type;
 	u8 event;
+	u8 candidate_count; /* #lzx */
 	bool real_access;
 };
 
+#define PARP_EFFECTIVE_TIER_ACCESS_TRACE_TIME_FORMAT \
+	"time=%llu trace_seq=%llu experiment=%llu session=%llu " /* #lzx */
+#define PARP_EFFECTIVE_TIER_ACCESS_TRACE_IDENTITY_FORMAT \
+	"cookie=%llu lifetime=%u gen=%d type=%u event=%u " /* #lzx */
+#define PARP_EFFECTIVE_TIER_ACCESS_TRACE_CANDIDATE_FORMAT \
+	"candidate_count=%u real=%d" /* #lzx */
+
 struct parp_effective_tier_outcome_trace {
 	u64 timestamp_ns;
 	u64 trace_sequence;
@@ -536,6 +544,7 @@ TRACE_EVENT(parp_effective_tier_access,
 		__field(s32, generation)
 		__field(u8, page_type)
 		__field(u8, event)
+		__field(u8, candidate_count) /* #lzx */
 		__field(bool, real_access)
 	),
 	TP_fast_assign(
@@ -548,14 +557,18 @@ TRACE_EVENT(parp_effective_tier_access,
 		__entry->generation = event->generation;
 		__entry->page_type = event->page_type;
 		__entry->event = event->event;
+		__entry->candidate_count = event->candidate_count; /* #lzx */
 		__entry->real_access = event->real_access;
 	),
-	TP_printk("time=%llu trace_seq=%llu experiment=%llu session=%llu cookie=%llu lifetime=%u gen=%d type=%u event=%u real=%d",
+	TP_printk(PARP_EFFECTIVE_TIER_ACCESS_TRACE_TIME_FORMAT
+		  PARP_EFFECTIVE_TIER_ACCESS_TRACE_IDENTITY_FORMAT
+		  PARP_EFFECTIVE_TIER_ACCESS_TRACE_CANDIDATE_FORMAT, /* #lzx */
 		  __entry->timestamp_ns, __entry->trace_sequence,
 		  __entry->experiment_id, __entry->session_id,
 		  __entry->folio_cookie,
 		  __entry->lifetime_epoch, __entry->generation,
-		  __entry->page_type, __entry->event, __entry->real_access)
+		  __entry->page_type, __entry->event, __entry->candidate_count,
+		  __entry->real_access) /* #lzx */
 );
 
 TRACE_EVENT(parp_effective_tier_outcome,
diff --git a/mm/parp/core/effective_tier.c b/mm/parp/core/effective_tier.c
index bfae35169..6a9cb6b44 100644
--- a/mm/parp/core/effective_tier.c
+++ b/mm/parp/core/effective_tier.c
@@ -446,14 +446,22 @@ void __parp_effective_tier_mglru_state_change(void)
 	atomic_set(&parp_tier_state_fault, 1);
 }
 
+bool parp_effective_tier_candidate_access_trace_eligible(bool real_access,
+		u8 candidate_count)
+{
+	return real_access && candidate_count; /* #lzx */
+} /* #lzx */
+
 static void parp_emit_access(struct folio *folio,
 		struct parp_reuse_page_ext *ext, enum parp_access_event event,
-		bool real_access)
+		bool real_access, u8 candidate_count) /* #lzx */
 {
 	struct parp_effective_tier_access_trace trace;
 	struct parp_tier_runtime_config config = { };
 
-	if (!trace_parp_effective_tier_access_enabled())
+	if (!trace_parp_effective_tier_access_enabled() ||
+	    !parp_effective_tier_candidate_access_trace_eligible(real_access,
+			candidate_count)) /* #lzx */
 		return;
 	trace = (struct parp_effective_tier_access_trace) {
 		.timestamp_ns = ktime_get_mono_fast_ns(),
@@ -463,6 +471,7 @@ static void parp_emit_access(struct folio *folio,
 		.generation = folio_lru_gen(folio),
 		.page_type = folio_is_file_lru(folio),
 		.event = event,
+		.candidate_count = candidate_count, /* #lzx */
 		.real_access = real_access,
 	};
 	if (parp_runtime_config_read(&config)) {
@@ -484,6 +493,7 @@ void __parp_effective_tier_note_access(struct folio *folio,
 	u32 new_state;
 	u32 decision_epochs;
 	u32 now = parp_now_ms();
+	u8 candidate_count = 0; /* #lzx */
 	bool real_access = parp_access_event_is_real(event);
 
 	if (event >= PARP_ACCESS_EVENT_NR)
@@ -503,6 +513,8 @@ void __parp_effective_tier_note_access(struct folio *folio,
 	if (real_access) {
 		bool initialized = old_state & PARP_STATE_INITIALIZED;
 		u8 access_ema = FIELD_GET(PARP_STATE_ACCESS_MASK, old_state);
+		candidate_count = FIELD_GET(PARP_STATE_CANDIDATE_MASK,
+			old_state); /* #lzx */
 		u32 previous = READ_ONCE(ext->last_access_ms);
 		u8 current_epoch = READ_ONCE(parp_tier_state_epoch);
 		bool new_run = FIELD_GET(PARP_TIER_STATE_EPOCH_MASK,
@@ -544,7 +556,7 @@ void __parp_effective_tier_note_access(struct folio *folio,
 	}
 	parp_state_write_end(ext, old_state, new_state);
 emit:
-	parp_emit_access(folio, ext, event, real_access);
+	parp_emit_access(folio, ext, event, real_access, candidate_count); /* #lzx */
 	page_ext_put(page_ext);
 }
 
@@ -584,7 +596,7 @@ void __parp_effective_tier_note_move(struct folio *folio,
 		WRITE_ONCE(ext->generation_enter_ms, parp_now_ms());
 	parp_state_write_end(ext, old_state, new_state);
 emit:
-	parp_emit_access(folio, ext, event, false);
+	parp_emit_access(folio, ext, event, false, 0); /* #lzx */
 	page_ext_put(page_ext);
 }
 
diff --git a/mm/parp/tests/parp_test.c b/mm/parp/tests/parp_test.c
index 76c4bb311..9e54a4682 100644
--- a/mm/parp/tests/parp_test.c
+++ b/mm/parp/tests/parp_test.c
@@ -1077,6 +1077,18 @@ static void parp_effective_recency_time_test(struct kunit *test)
 	KUNIT_EXPECT_EQ(test, parp_effective_tier_metadata_size(), 24UL);
 }
 
+static void parp_effective_candidate_access_trace_gate_test(struct kunit *test)
+{
+	KUNIT_EXPECT_FALSE(test,
+		parp_effective_tier_candidate_access_trace_eligible(false, 1)); /* #lzx */
+	KUNIT_EXPECT_FALSE(test,
+		parp_effective_tier_candidate_access_trace_eligible(true, 0)); /* #lzx */
+	KUNIT_EXPECT_TRUE(test,
+		parp_effective_tier_candidate_access_trace_eligible(true, 1)); /* #lzx */
+	KUNIT_EXPECT_TRUE(test,
+		parp_effective_tier_candidate_access_trace_eligible(true, 63)); /* #lzx */
+} /* #lzx */
+
 static void parp_effective_access_lifetime_test(struct kunit *test)
 {
 	struct parp_tier_state_snapshot before = { };
@@ -1317,6 +1329,7 @@ static struct kunit_case parp_cases[] = {
 	KUNIT_CASE(parp_effective_tier_pressure_policy_test), /* #lzx */
 	KUNIT_CASE(parp_effective_random_matched_test),
 	KUNIT_CASE(parp_effective_recency_time_test),
+	KUNIT_CASE(parp_effective_candidate_access_trace_gate_test), /* #lzx */
 	KUNIT_CASE(parp_effective_access_lifetime_test),
 	KUNIT_CASE(parp_effective_epoch_limit_test),
 	KUNIT_CASE(parp_effective_trace_quadrants_test),
diff --git a/tools/parp/effective_tier/collector.py b/tools/parp/effective_tier/collector.py
index 695d10633..0af91bdaa 100644
--- a/tools/parp/effective_tier/collector.py
+++ b/tools/parp/effective_tier/collector.py
@@ -349,6 +349,8 @@ def parse_exported_trace(paths: Sequence[Path]) -> Tuple[List[Dict[str, object]]
                                 fields, "lifetime", context),
                             "event": _trace_int(fields, "event", context),
                             "real": bool(_trace_int(fields, "real", context)),
+                            "candidate_count": _trace_optional_int(
+                                fields, "candidate_count", context),  #lzx
                         })
                     elif kind == "outcome":
                         context = "effective-tier outcome"
@@ -459,6 +461,10 @@ def parse_exported_trace(paths: Sequence[Path]) -> Tuple[List[Dict[str, object]]
         if not raw["real"]:
             stats["policy_move_access_events_ignored"] += 1
             continue
+        if raw["candidate_count"] is None or int(
+                raw["candidate_count"]) < 1:  #lzx
+            raise ContractError(
+                "real access trace lacks positive candidate_count")  #lzx
         source = _trace_enum(TRACE_ACCESS, int(raw["event"]), "real access")
         key = (str(raw["folio_cookie"]), int(raw["folio_lifetime_epoch"]))
         if raw["experiment_id"] is not None and raw["session_id"] is not None:
@@ -482,6 +488,7 @@ def parse_exported_trace(paths: Sequence[Path]) -> Tuple[List[Dict[str, object]]
             "folio_lifetime_epoch": int(raw["folio_lifetime_epoch"]),
             "access_source": source,
             "is_real_access": True,
+            "candidate_count": int(raw["candidate_count"]),  #lzx
         })
         stats["real_access_events"] += 1
 
diff --git a/tools/parp/effective_tier/contracts.py b/tools/parp/effective_tier/contracts.py
index 9d757e78d..05b033056 100644
--- a/tools/parp/effective_tier/contracts.py
+++ b/tools/parp/effective_tier/contracts.py
@@ -359,7 +359,7 @@ def validate_access(record: Mapping[str, object]) -> None:
     _require(record, (
         "schema_version", "event_kind", "timestamp_ns", "experiment_id",
         "session_id", "folio_cookie", "folio_lifetime_epoch",
-        "access_source", "is_real_access",
+        "access_source", "is_real_access", "candidate_count",  #lzx
     ), "real access")
     if record["schema_version"] != SCHEMA_VERSION:
         raise ContractError("unsupported access schema_version")
@@ -371,6 +371,8 @@ def validate_access(record: Mapping[str, object]) -> None:
     _integer(record["folio_lifetime_epoch"], "folio_lifetime_epoch", 0)
     if _boolean(record["is_real_access"], "is_real_access") is not True:
         raise ContractError("future labels require a real access signal")
+    if _integer(record["candidate_count"], "candidate_count", 1) < 1:  #lzx
+        raise ContractError("future labels require candidate-correlated access")  #lzx
     if record["access_source"] not in REAL_ACCESS_SOURCES:
         raise ContractError("policy/generation moves cannot label future reuse")
 
diff --git a/tools/parp/effective_tier/raw_event.schema.json b/tools/parp/effective_tier/raw_event.schema.json
index 43cef5d84..106da17be 100644
--- a/tools/parp/effective_tier/raw_event.schema.json
+++ b/tools/parp/effective_tier/raw_event.schema.json
@@ -220,7 +220,8 @@
     "real_access": {
       "type": "object",
       "additionalProperties": true,
-      "required": ["schema_version", "event_kind", "timestamp_ns", "experiment_id", "session_id", "folio_cookie", "folio_lifetime_epoch", "access_source", "is_real_access"],
+      "$comment": "#lzx: real accesses must be emitted only after a candidate gate observation",
+      "required": ["schema_version", "event_kind", "timestamp_ns", "experiment_id", "session_id", "folio_cookie", "folio_lifetime_epoch", "access_source", "is_real_access", "candidate_count"],
       "properties": {
         "schema_version": { "const": 2 },
         "event_kind": { "const": "real_access" },
@@ -230,7 +231,8 @@
         "folio_cookie": { "$ref": "#/$defs/identifier" },
         "folio_lifetime_epoch": { "type": "integer", "minimum": 0 },
         "access_source": { "enum": ["PTE_YOUNG", "MARK_ACCESSED", "FD_REFERENCE"] },
-        "is_real_access": { "const": true }
+        "is_real_access": { "const": true },
+        "candidate_count": { "type": "integer", "minimum": 1, "$comment": "#lzx: count captured before the real access clears candidate state" }
       }
     }
   }
diff --git a/tools/parp/effective_tier/tests/test_phase_e.py b/tools/parp/effective_tier/tests/test_phase_e.py
index 5e35d15eb..6424cfb6c 100644
--- a/tools/parp/effective_tier/tests/test_phase_e.py
+++ b/tools/parp/effective_tier/tests/test_phase_e.py
@@ -153,6 +153,7 @@ def access(session: str, cookie: str, timestamp_ns: int,
         "folio_lifetime_epoch": lifetime,
         "access_source": source,
         "is_real_access": True,
+        "candidate_count": 1,  #lzx
     }
 
 
@@ -245,7 +246,7 @@ class PhaseECollectorTests(unittest.TestCase):
             "pressure_bypass_reason=0",  #lzx
             "task: parp_effective_tier_access: time=1050000000 trace_seq=8 "
             "experiment=1 session=2 cookie=99 "
-            "lifetime=7 gen=0 type=1 event=0 real=1",
+            "lifetime=7 gen=0 type=1 event=0 candidate_count=1 real=1",  #lzx
             "task: parp_effective_tier_access: time=1055000000 trace_seq=9 "
             "experiment=1 session=2 cookie=99 "
             "lifetime=7 gen=1 type=1 event=6 real=0",
@@ -514,6 +515,12 @@ class PhaseECollectorTests(unittest.TestCase):
         with self.assertRaises(ContractError):
             validate_access(event)
 
+    def test_uncorrelated_real_access_cannot_be_a_future_label(self):
+        event = access("s", "f", 123)
+        event["candidate_count"] = 0  #lzx
+        with self.assertRaisesRegex(ContractError, "candidate_count"):
+            validate_access(event)
+
     def test_trace_loss_is_measured_not_inferred(self):
         row = candidate("s", "f", "KEEP_RECLAIM", 1_000_000_000)
         meta = session("s", "train", 1, lost=2)
-- 
2.25.1

