# lzx-note: Match the unique device name even during early built-in init, when
# sysfs may expose it at /sys/devices/myfs before the misc class is populated.
KERNEL=="myfs", MODE="0660", TAG+="uaccess"
