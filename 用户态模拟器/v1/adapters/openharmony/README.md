# OpenHarmony adapter boundary

Not implemented in v1. A later adapter must first verify the target OpenHarmony kernel and memory-accounting interfaces, then provide platform and executor ops around the existing reclaim mechanisms. This directory contains no kernel implementation.
