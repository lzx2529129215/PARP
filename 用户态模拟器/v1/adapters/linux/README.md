# Linux adapter boundary

Not implemented in v1. A later adapter may query Shadow policy metadata from the existing Linux global-LRU reclaim path. Real kernel page state, isolation, putback, reverse mapping, writeback, swap, unmap and freeing remain authoritative and are not reimplemented here.
